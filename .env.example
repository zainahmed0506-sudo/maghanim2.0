# Canonical origin, used for metadataBase, hreflang alternates, canonicals,
# sitemap.xml and robots.txt. No trailing slash.
# Production value pending client confirmation of the domain (CLAUDE.md §13).
NEXT_PUBLIC_SITE_URL="https://maghanim.ae"

# --- Phase 6 (contact form) — not used yet ---
# Transactional email provider API key. Set in Vercel only, never committed.
# RESEND_API_KEY=""
# Address that contact-form submissions are delivered to.
# CONTACT_RECIPIENT_EMAIL=""
